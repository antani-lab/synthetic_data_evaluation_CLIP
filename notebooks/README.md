# Exploratory notebooks

These notebooks were moved from the original repository root and retained for provenance:

- `coverage_biomedclip.ipynb`
- `constraint_biomedclip.ipynb`
- `consistency_biomedclip.ipynb`

They may contain historical local paths and definitions. For analyses aligned with the finalized manuscript, use the reusable modules in `src/synthetic_data_evaluation/` and the command-line scripts in `scripts/`.
