# Algorithm supplements

- `algorithm_S1.pdf`: coverage analysis in BiomedCLIP embedding space.
- `algorithm_S2.pdf`: centroid-based constraint and consistency analysis.
- `*_legacy.tex`: algorithm descriptions preserved from the original repository.

The reusable Python implementation follows the finalized manuscript if the historical TeX or notebooks differ. It reports the signed synthetic-minus-real mean gap and dispersion ratio for consistency and does not perform the historical ANOVA.
